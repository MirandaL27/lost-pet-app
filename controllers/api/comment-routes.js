const router = require('express').Router();
//const { Comment } = require('../../models');

router.get('/', (req, res) => {
   //find all comments
});

router.post('/',(req, res) => {
  //post comments
});

module.exports = router;